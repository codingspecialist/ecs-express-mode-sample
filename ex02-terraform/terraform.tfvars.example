# 이 파일을 terraform.tfvars 로 복사한 뒤 값을 조정하세요.
# (기본값으로도 동작하므로 그대로 두어도 됩니다)

aws_region          = "ap-northeast-2"
project             = "ecs-demo"
image_tag           = "latest"
basic_desired_count = 2
auth_desired_count  = 1
